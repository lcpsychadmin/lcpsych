<?xml version="1.0" encoding="UTF-8"?>
<location>
  <name>{{ contact.heading|default:"L+C Psychological Services" }}</name>
  <website>{{ site_base }}/</website>
  <address>
    {% for line in contact.office_address.splitlines %}
    <line>{{ line|escape }}</line>
    {% endfor %}
  </address>
  <phone>{{ contact.phone_number }}</phone>
  <fax>{{ contact.fax_number }}</fax>
  <email>{{ contact.email_address }}</email>
  <hours>
    {% for line in contact.office_hours.splitlines %}
    <rule>{{ line|escape }}</rule>
    {% endfor %}
  </hours>
  <mapEmbed>{{ contact.map_embed_url }}</mapEmbed>
  <directions>{{ contact.directions_url }}</directions>
  <schedule>{{ contact.cta_url }}</schedule>
</location>
