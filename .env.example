# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=

# Database (Heroku supplies DATABASE_URL)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME

# SEO / Canonical domain
# Public canonical base URL (no trailing slash)
BASE_URL=https://www.lcpsych.com
# Allow robots in production (set to true in production)
ROBOTS_ALLOW=False

# AWS S3 (media storage)
# Provide these in production to enable S3-backed media storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1
# Optional: if using CloudFront or a custom domain for the bucket
# AWS_S3_CUSTOM_DOMAIN=cdn.example.com
# Optional: include signed querystrings for private buckets (default False)
# AWS_QUERYSTRING_AUTH=False

# IP geolocation (ipinfo.io token for analytics location fallback)
IPINFO_TOKEN=
